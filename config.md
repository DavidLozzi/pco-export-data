To use the config, create a config.py file, and copy/paste the following:

```
API_USERNAME = 'specify your user name'
API_PASSWORD = 'specify your password'
```

The config.py file is ignored by git.